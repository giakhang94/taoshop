//reudcer cho product provider using useReducer & useContext

const initProductStates = {
    data: [],
};

const productReducer = (state, action) => {
    switch (action.type) {
        default:
            console.log('error');
    }
};
export { initProductStates, productReducer };
